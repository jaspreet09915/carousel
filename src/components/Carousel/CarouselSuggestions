// The commented code below is for a timer
    // It is working, but it could move right after a manual slide change
    // To be completed at a later stage
    // constructor(props){
    //     super(props)
    //     this.makeTimer();
    // }
   
    // makeTimer(){
    //     setInterval(() => {
    //         console.log('timer')
    //         this.nextSlideHandler()
    //     }, this.props.timer !== undefined ? this.props.timer: 2000)
    // }